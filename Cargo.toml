[package]
name = "load_plugin"
version = "0.1.0"
edition = "2021"

[lib]
name = "load_plugin"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20.0", features = ["extension-module"] }
wasmedge-sdk = { version = "0.13.2", features = ["wasi_nn"] }

